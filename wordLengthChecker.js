//Take a word as input and return its lenght

const theWord = "usmanqw"

const worldLengthChecker = function(word){
  let calculate = word.length
  return calculate
}

const totalLength = worldLengthChecker(theWord)
console.log(totalLength)